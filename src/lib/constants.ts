// Fish species are now stored in the species_dictionary table in Supabase.
// Use useSpeciesStore to access them.

export const MAPBOX_DEFAULT_CENTER = {
  longitude: 101.6869, // KL as fallback
  latitude: 3.1390,
  zoom: 12,
};
